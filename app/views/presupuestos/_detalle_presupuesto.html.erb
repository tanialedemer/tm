<% show_actions = show_actions.nil? ? false : show_actions %>
<hr>
<div class="table-responsive">
	<table class="table table-condensed">
		<thead>
            <tr>
				<td><strong>Descripción</strong></td>
				<td class="text-right"><strong>Cantidad</strong></td>
				<td class="text-right"><strong>Precio</strong></td>
				<td class="text-right"><strong>Subtotal</strong></td>
				<%= show_actions ? '<td colspan="2"></td>'.html_safe : "" %>
            </tr>
		</thead>
		<tbody>
			<div>
				<%= render partial: "detalle_orden_presupuestos/index", locals: {show_actions: show_actions} %>
			</div>
			<tr>
				<td class="thick-line"></td>
				<td class="thick-line"></td>
				<td class="thick-line"></td>
        <td class="thick-line"></td>
				<td class="thick-line text-right"><h4>Total</h4></td>
				<td class="thick-line text-right"><h4><%= number_to_currency(@presupuesto.total, unit: "Gs.") %></h4></td>
				<%= hidden_field_tag :total  %>
				<%= show_actions ? '<td colspan="2" class=""></td>'.html_safe : "" %>
			</tr>
		</tbody>
	</table>
</div>
