<%= form_with(model: tipo_factura, local: true) do |form| %>
  <% if tipo_factura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_factura.errors.count, "error") %> prohibited this tipo_factura from being saved:</h2>

      <ul>
        <% tipo_factura.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
