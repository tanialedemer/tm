<div class="container">

<h3 align="center">Usuarios</h3>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Usuario</th>
      <th>Rol</th>
      <th colspan="3">
        <%= link_to 'Nuevo Presupuesto', new_user_path, class:"btn btn-success" %>
      </th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.username %></td>
        <td><%#= link_to 'Detalles', presupuesto_factura_path(presupuesto), class:"btn btn-info" %></td>
        <td><%#= link_to 'Editar', edit_presupuesto_path(presupuesto), class:"btn btn-primary" %></td>
        <td><%= link_to 'Eliminar', user, method: :delete, data: { confirm: 'Está seguro?' }, class:"btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
