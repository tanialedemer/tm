<div class="container">
<%= form_with(model: compra, remote: true) do |form| %>
  <% if compra.errors.any? %>
    <div id="error_explanation">
      <%= pluralize(compra.errors.count, "errores") %>

      <ul>
        <% compra.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-6">
  <div class="form-group">
    <%= form.label :fecha %>
    <%= form.date_field :fecha %>
  </div>
</div>
<div class="col-6">
  <div class="form-group">
    <%= form.label :num_fact, 'Número' %>
    <%= form.number_field :num_fact %>
  </div>
</div>
</div>
 <div class="form-group">
    <%= form.label :proveedor_id ,"Proveedor"%> <br>
    <%= form.collection_select :proveedor_id, Proveedor.order(:nombre), :id, :nombre,class: "chosen-select form-control" %>
  </div>

  <div class="actions">
    <%= form.submit 'Siguiente', class: 'form-control btn btn-success' %>
  </div>
<% end %>
</div>